# Project instructions

When working in the command-center layout, read `../AGENTS.md` and `../STATE.md` first.
A standalone clone must work without those private files.
Use real measurements only; keep credentials, caches and private planning out of commits.
Run `python -m unittest discover -s tests -v` before pushing code changes.
Do not claim sponsor integration until its adapter has executed successfully.
